machine:
  pre:
    - sudo apt-get install clang-3.6 lldb-3.6 mono-devel
    - sudo apt-get install dotnet
    - mozroots --import --sync
    - source /home/ubuntu/.dnx/dnvm/dnvm.sh
    - dnvm install latest -r coreclr -u

  
dependencies:

  post:
    - dnu restore
